{
  "scripts": {
    "get-submodules": "git submodule update --init --recursive --depth 1",
    "preinstall": "npm run get-submodules"
  },
  "devDependencies": {
    "autoprefixer": "^10.3.1",
    "netlify-cli": "^3.39.4",
    "postcss": "^8.3.6",
    "postcss-cli": "^8.3.1"
  }
}
