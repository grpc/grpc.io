[build]
publish = "public"
command = "make production-build"

[build.environment]
HUGO_VERSION = "0.82.0"

[context.branch-deploy]
command = "make preview-build"

[context.deploy-preview]
command = "make preview-build"
